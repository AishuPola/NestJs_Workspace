import { iopCommonUtilities } from './iop-common-utilities.js';

describe('iopCommonUtilities', () => {
  it('should work', () => {
    expect(iopCommonUtilities()).toEqual('iop-common-utilities');
  });
});
