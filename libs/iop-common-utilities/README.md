# iop-common-utilities

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build iop-common-utilities` to build the library.

## Running unit tests

Run `nx test iop-common-utilities` to execute the unit tests via [Jest](https://jestjs.io).
